import 'package:flutter/material.dart';

class MainLoadData_Android extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}
