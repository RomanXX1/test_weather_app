import 'package:flutter/cupertino.dart';

class MainLoadData_iOS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: CupertinoActivityIndicator(),
    );
  }
}
